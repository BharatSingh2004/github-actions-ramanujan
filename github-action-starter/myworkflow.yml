name: print hello world
on:
  push:
jobs:
  print:
    runs-on: macos-latest
    steps:
      - name: print hello world
        run: echo "Hello World"
  second_print:
    runs-on: macos-latest
    steps:
      - name: Install Dependencies
        run: npm ci
      - name: run test
        run: npm test